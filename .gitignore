# Секрети — ключ Nexus Mods лежить у .env, ніколи не комітити
.env

# Гра та її асембліки — копірайт ConcernedApe, в репо їм не місце
*.apk
*.apks
libs/

# Артефакти збірки
bin/
obj/
*.user

# Розпакований Android-порт (гра, копірайт)
android/
